# InventoryTrackingAutomation

This library was generated with [Angular CLI](https://github.com/angular/angular-cli) version 12.0.4.

## Code scaffolding

Run `ng generate component component-name --project inventory-tracking-automation` to generate a new component. You can also use `ng generate directive|pipe|service|class|guard|interface|enum|module --project inventory-tracking-automation`.
> Note: Don't forget to add `--project inventory-tracking-automation` or else it will be added to the default project in your `angular.json` file. 

## Build

Run `ng build inventory-tracking-automation` to build the project. The build artifacts will be stored in the `dist/` directory.

## Publishing

After building your library with `ng build inventory-tracking-automation`, go to the dist folder `cd dist/inventory-tracking-automation` and run `npm publish`.

## Running unit tests

Run `ng test inventory-tracking-automation` to execute the unit tests via [Karma](https://karma-runner.github.io).

## Further help

To get more help on the Angular CLI use `ng help` or go check out the [Angular CLI Overview and Command Reference](https://angular.io/cli) page.
