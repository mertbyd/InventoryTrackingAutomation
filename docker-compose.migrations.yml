# EF Core migration'larını çalıştırmak için kullanılır.
# Kullanım:
#   docker-compose up -d                          # önce postgres ayağa kaldır
#   cd src/InventoryTrackingAutomation.EntityFrameworkCore
#   dotnet ef database update --startup-project ../../../host/InventoryTrackingAutomation.HttpApi.Host

# Bu dosya şu an placeholder olarak tutulmaktadır.
# Migration'lar manuel olarak yukarıdaki komutla çalıştırılır.
version: '3.8'
