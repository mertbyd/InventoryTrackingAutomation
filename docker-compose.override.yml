# docker-compose.yml üzerine local override'lar.
# Gerekirse buraya ek servisler (Redis, pgAdmin vb.) eklenebilir.

version: '3.8'

services:
  postgres:
    environment:
      POSTGRES_PASSWORD: postgres
